<Window x:Class="RemoteExplorerWindowClient.RemoteExplorerWindowClientMainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:RemoteExplorerWindowClient"
        mc:Ignorable="d"
        Title="RemoteExplorerWindowClientMainWindow" Height="350" Width="525">

    <Window.DataContext>
        <local:RemoteExplorerWindowClientViewModel />
    </Window.DataContext>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Button Grid.Row="0" Content="Connect" Command="{Binding ConnectAsyncCommand}" />

        <TextBox Grid.Row="1" Text="{Binding SelectedEntry, UpdateSourceTrigger=PropertyChanged}" />

        <ListBox Grid.Row="2"
                 ItemsSource="{Binding Entries}"
                 SelectedItem="{Binding SelectedEntry}" />


        <Button Grid.Row="3" Content="Enumerate" Command="{Binding EnumerateFolderAsyncCommand}" />

        <TextBlock Grid.Row="4" Text="{Binding State}" />
    </Grid>
</Window>